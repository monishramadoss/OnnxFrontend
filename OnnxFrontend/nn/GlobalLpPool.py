import numpy as np

class GlobalLpPool_1:

	p = m_float()
	def __init__(self, _name: str, p: float):
		self.name = _name
		self.m_p = p

	def __call__(self, X: str):
		 return Y


class GlobalLpPool_2:

	p = m_int()
	def __init__(self, _name: str, p: int):
		self.name = _name
		self.m_p = p

	def __call__(self, X: str):
		 return Y
