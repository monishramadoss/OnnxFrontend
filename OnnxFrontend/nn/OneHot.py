import numpy as np

class OneHot_9:

	axis = m_int()
	def __init__(self, _name: str, axis: int):
		self.name = _name
		self.m_axis = axis

	def __call__(self, indices: str, depth: str, values: str):
		 return output
