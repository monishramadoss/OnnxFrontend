import numpy as np

class Scatter_9:

	axis = m_int()
	def __init__(self, _name: str, axis: int):
		self.name = _name
		self.m_axis = axis

	def __call__(self, data: str, indices: str, updates: str):
		 return output
