import numpy as np

class Split_1:

	axis = m_int()
	split = m_list()
	def __init__(self, _name: str, axis: int, split: list):
		self.name = _name
		self.m_axis = axis
		self.m_split = split

	def __call__(self, input: str, split: str):
		 return outputs...


class Split_2:

	axis = m_int()
	split = m_list()
	def __init__(self, _name: str, axis: int, split: list):
		self.name = _name
		self.m_axis = axis
		self.m_split = split

	def __call__(self, input: str):
		 return outputs
