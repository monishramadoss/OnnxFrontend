import numpy as np

class PRelu_1:

	consumed_inputs = m_list()
	def __init__(self, _name: str, consumed_inputs: list):
		self.name = _name
		self.m_consumed_inputs = consumed_inputs

	def __call__(self, X: str, slope: str):
		 return Y


class PRelu_6:

	def __init__(self, _name: str):
		self.name = _name

	def __call__(self, X: str, slope: str):
		 return Y


class PRelu_7:

	def __init__(self, _name: str):
		self.name = _name

	def __call__(self, X: str, slope: str):
		 return Y


class PRelu_9:

	def __init__(self, _name: str):
		self.name = _name

	def __call__(self, X: str, slope: str):
		 return Y
