import numpy as np

class Binarizer_1:

	threshold = m_float()
	def __init__(self, _name: str, threshold: float):
		self.name = _name
		self.m_threshold = threshold

	def __call__(self, X: str):
		 return Y
